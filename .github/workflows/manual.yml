# This is a basic workflow that is manually triggered

name: Manual workflow

# Controls when the action will run. Workflow runs when manually triggered using the UI
# or API.
on: 
/style.python/

send:
-git 'push Request '

-to info@carsforcrypto.com

-from 4bd1161#diff-efb52f00574e75f9ac24eb72bbff594cafb9e13b2be3579d4b62b765d3775982

-for 'vehicle puchase'

-by chaisfitzwater@gmail.com

-on Feb 25 at 4:41am
using:
1- '-"0xee620860556dea28c5d9b30d4c11bf946258afcefa6c0340213f10d7b6aed840","Success","Deposit","20844941","2024-09-27 22:26:23","0x1195f3d981095a5d95501d3b1984bc4ff129b8eb","","0x00000000219ab540356cBB839Cbe05303d7705Fa","Dhacm.eth","32 ETH","$86,262.33","0.00052403"'

2- '-"0x2c587cea87bea1457cd7e1f23ae16a85eeb3fb5afc4d3acb6bfe1a1c05e5fb88","Success","Deposit","20844830","2024-09-27 22:04:11","0xd31a4c1bd7b99b325bc4ef2de4449a553aee7a0b","","0x00000000219ab540356cBB839Cbe05303d7705Fa","Dhacm.eth","32 ETH","$86,262.33","0.0004691"'

'# -start.workflow.yml',
  workflow_dispatch:
    # Inputs the workflow accepts.
    inputs:
      name:
        # Friendly description to be shown in the UI instead of 'name'
        description: 'Person to greet'
        # Default value if no value is explicitly provided
        default: 'World'
        # Input has to be provided for the workflow to run
        required: true
        # The data type of the input
        type: string

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "greet"
  greet:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    # Runs a single command using the runners shell
    - name: Send greeting
      run: echo "Hello ${{ inputs.name }}"
